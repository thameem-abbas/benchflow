apiVersion: benchflow.io/v1alpha1
kind: DeploymentProfile
metadata:
  name: llm-d-optimized-baseline-ibmcluster
spec:
  platform: llm-d
  mode: inference-scheduling
  repo_url: https://github.com/llm-d/llm-d.git
  repo_ref: v0.7
  gateway: istio
  endpoint_path: /v1/models
  model_storage:
    pvc_name: models-storage
    cache_dir: /models
    mount_path: /model-cache
  runtime:
    replicas: 8
    tensor_parallelism: 2
    env:
      TRTLLM_TMPDIR: /tmp/tensorrt_llm
      VLLM_SHM_BROADCAST_TIMEOUT_SECONDS: "300"
    vllm_args:
      - --gpu-memory-utilization=0.90
      - --block-size=64
